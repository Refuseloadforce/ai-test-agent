# AI Config-Driven Desktop Automation Agent dependencies
# NOTE: keep this file ASCII-only (no Chinese comments), otherwise pip
# on Windows fails with UnicodeDecodeError (GBK). Use English only.

# Screen capture
mss>=9.0.1

# Mouse & keyboard control
pyautogui>=0.9.54
pynput>=1.7.6
pyperclip>=1.8.0

# Image processing (screenshots for reports)
Pillow>=10.0.0

# DeepSeek API (OpenAI-compatible REST, no local model needed)
requests>=2.31.0

# Logging
loguru>=0.7.0

# Windows OCR (for click_text / wait_text actions; Windows only)
winsdk>=1.0.0; platform_system == "Windows"

# Browser control via Chrome DevTools Protocol (browser_* actions)
websocket-client>=1.7.0
